// Copy this file to `demo_credentials.dart` (which is gitignored) and fill in your values.
// The example app reads its credentials from there so nothing sensitive is ever committed.
const String demoApiKey = '<YOUR_PUBLISHABLE_KEY>'; // pk_live_...
const String demoUserToken = '<A_REAL_END_USER_TOKEN>'; // pushed to the SDK as the bearer
const String? demoAgentUrl = null; // null -> baked production endpoint; or 'http://127.0.0.1:8001'
